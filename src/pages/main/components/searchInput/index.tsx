import * as s from './style';
import { Button } from '@/common';

export const SearchInput = () => {
  return (
    <s.SerachInput>
      <s.Input type="text" />
      <Button>hello</Button>
    </s.SerachInput>
  );
};
