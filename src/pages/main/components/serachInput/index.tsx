import { Button } from '@/common';
import * as s from './style';

export const SerachInput = () => {
  return (
    <s.SerachInput>
      <s.Input type="text" />
      <Button>hello</Button>
    </s.SerachInput>
  );
};
